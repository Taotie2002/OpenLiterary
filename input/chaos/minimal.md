# Chaos Fixture 01: Minimal Input (Single Short Paragraph)

**Target feature**: chunker must handle tiny inputs gracefully
**Expected**: 1 chunk, no hard-cut warnings

A single short paragraph that tests whether the chunker gracefully handles minimal input. We expect exactly one chunk out.
